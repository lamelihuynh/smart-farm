
  # Smart farm

  This is a code bundle for Smart farm. The original project is available at https://www.figma.com/design/AiKnETugNf98CMicdC3FeV/Smart-farm.

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.
  
